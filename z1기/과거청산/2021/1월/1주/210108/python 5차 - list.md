# python

## list 
```python
  len(list) #list 요소 갯수
  list.append(값) #추가
  del list[index] #삭제
  list.insert(index,값)
  #정렬된 새로운 list를 return
  sorted(list) #오름차순
  sorted(list, reverse = True) #내림차순
  #list += 정렬된 list
  list.sort()
```

