### npm 오류

npm install을 할 때 ELIFECYCLE 오류

```
$ npm cache clean --force
$ rm -rf node_modules package-lock.json
$ npm install
```



#### npm install 중 에러 찾기

```npm install --verbose ``` 명령어를 쳐서 오류명 알아내기
